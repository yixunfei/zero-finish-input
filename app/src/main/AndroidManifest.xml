<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".ZeroInputApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZeroInput"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".settings.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.UserDictionaryActivity"
            android:exported="false"
            android:parentActivityName=".settings.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".settings.SecureClipboardManagerActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:parentActivityName=".settings.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".expressions.ExpressionManagerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.ZeroInput.PrivateImport"
            android:parentActivityName=".settings.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".auth.SecureClipboardUnlockActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:taskAffinity=""
            android:theme="@style/Theme.ZeroInput.Authentication" />

        <activity
            android:name=".clipboard.ClipboardImportActivity"
            android:label="@string/copy_to_zeroinput"
            android:launchMode="singleTask"
            android:exported="true"
            android:excludeFromRecents="true"
            android:allowTaskReparenting="false"
            android:taskAffinity=""
            android:theme="@style/Theme.ZeroInput.PrivateImport"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".ZeroInputService"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|locale|layoutDirection|uiMode"
            android:directBootAware="false"
            android:exported="true"
            android:label="@string/input_method_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:theme="@style/Theme.ZeroInput.InputMethod">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name=".clipboardguard.ClipboardGuardSettingsActivity"
            android:exported="false"
            android:parentActivityName=".settings.MainActivity" />
        <activity
            android:name=".clipboardguard.ClipboardClearActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.ZeroInput.PrivateImport" />

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            tools:ignore="MissingClass"
            tools:node="remove" />
    </application>

</manifest>
