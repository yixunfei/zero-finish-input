# ZeroInput 工程协作规范

本文件适用于仓库根目录及所有子目录。自动化代理、维护者和贡献者在修改代码前都必须遵守本文件；若子目录存在更具体的 `AGENTS.md`，仅可补充该目录规则，不得削弱这里的安全、隐私与架构边界。

## 1. 项目使命与决策优先级

ZeroInput 是一个面向 Android 的开源输入法，应用 ID 为 `dev.zeroinput.ime`，采用 Apache-2.0 许可证。首版以中文全拼和英语输入为核心，并提供本地用户词组、emoji 搜索与最近使用、安全剪贴板及数据导入导出能力；后续语言应通过语言包或可替换引擎扩展。

发生取舍时严格按以下顺序决策：

1. 用户隐私与数据安全。
2. 输入正确性、会话隔离和数据完整性。
3. 按键响应、稳定性和离线可用性。
4. 可维护、可测试、可替换的模块边界。
5. 功能丰富度和视觉表现。

安全或隐私要求不是可用体验换取的优化项。任何会削弱这些要求的功能都不得合入，即使它默认关闭。

## 2. 开始任务前

### 2.1 先理解再修改

- 先阅读与任务直接相关的实现、测试、模块 `build.gradle.kts`，以及 `docs/architecture.md`、`docs/threat-model.md` 中对应章节。
- 涉及引擎或安全剪贴板时，还必须阅读 `docs/adr/0001-engine-boundary.md` 或 `docs/adr/0002-private-clipboard.md`。
- 先确认用户目标、非目标、影响模块、数据边界和验收方式。存在会改变产品行为、数据格式、安全模型或架构方向的歧义时，必须向用户确认后再编码。
- 复杂任务先给出可审查的计划；仅在计划和关键取舍已获确认后实施。用户已明确“按推荐方案执行”时，不重复询问已确认事项。
- 检查工作区已有内容并保留用户改动。仓库可能没有 Git 元数据；不得把 `git diff` 或干净工作区作为工作的前提。

### 2.2 控制范围

- 只修改完成当前目标所需的文件，不顺带重构无关代码、不批量改格式、不进行无需求依据的兼容处理。
- 当前不是已发布的线上兼容项目。除非用户明确要求，开发阶段直接更新内部实现，不引入双写、旧格式迁移或长期兼容层；涉及已持久化数据格式时必须先确认迁移策略。
- 模块相互独立且任务庞杂时，只有在用户同意后才可并行委派。并行工作必须预先划分文件所有权、公共接口修改顺序和验证责任，避免同文件写入、Gradle/NDK 锁竞争和中间状态互相破坏。

## 3. 不可破坏的隐私与安全边界

以下均为强制要求。

### 3.1 零联网与零追踪

- Android Manifest 不得声明 `android.permission.INTERNET`，也不得添加可用于运行时联网的其他权限。
- 不得加入网络客户端、WebView 联网路径、广告 SDK、遥测、统计、设备指纹、崩溃上报、远程配置或推送服务。
- 输入、候选、词频、应用包名、剪贴板内容、诊断信息和使用行为不得离开设备。
- 核心输入、学习、emoji、安全剪贴板和语言包必须在完全离线状态下工作。
- 不得以“可选”“匿名”或“仅 Debug”为理由绕过上述规则。

### 3.2 系统剪贴板隔离

- IME 服务及其他业务代码不得获取、监听、读取或写入 Android 系统剪贴板。
- 禁止使用 `ClipboardManager`、`CLIPBOARD_SERVICE`、`getPrimaryClip`、`setPrimaryClip` 及等价反射或间接封装。
- 安全剪贴板是 ZeroInput 私有加密内容库，不与系统剪贴板同步，不后台采集内容，不导出 `ContentProvider`、Service 或其他跨应用读取通道。
- 内容只能由用户在 ZeroInput 管理页主动添加，并在输入法面板中主动选择后提交。

### 3.3 备份、导出与日志

- 保持 `android:allowBackup="false"`，云备份和设备迁移规则必须继续排除全部应用数据。
- 密钥、密文、明文、输入内容、候选内容、词组、包名和认证状态不得写入日志、异常文本、分析事件或测试快照。
- 用户主动导出才可产生文件。导出范围、格式、明文风险和覆盖行为必须明确；不得自动上传或共享。
- Release 构建不得可调试。秘密内容页面必须使用 `FLAG_SECURE`，且不得出现在最近任务或系统预览中。

### 3.4 编辑器隐私策略

- 密码、PIN、可见密码以及编辑器请求 `IME_FLAG_NO_PERSONALIZED_LEARNING` 时，必须禁用学习与个性化读取。
- 邮箱、URI、隐身模式或用户全局关闭学习时，同样不得读取个人词组或 emoji 最近记录。
- 隐私策略在会话开始、输入视图恢复和设置变化后的下一次交互时重新求值。权限收紧必须立即取消组合、隐藏旧候选、提升异步任务代次并使待处理写入失效。
- 新增任何编辑器分类时采用保守默认值；无法确定是否敏感时按敏感输入处理。

## 4. 加密与认证边界

- 本地敏感数据使用 Android Keystore 管理密钥，并使用 AES-256-GCM 提供机密性和完整性；不得自制密码学算法、固定 IV、硬编码密钥或把密钥写入文件。
- 用户词库、emoji 历史和安全剪贴板必须使用相互隔离的密钥别名，不能为了简化实现共享密钥。
- 密文和相关索引必须位于 `noBackupFilesDir`。认证前的安全剪贴板列表只能读取不含正文与标签的独立索引。
- 解密出的明文字节数组、字符缓冲区和临时副本应缩短生命周期，并在使用后尽快清零；不得建立明文磁盘缓存或长期进程缓存。
- 清除用户数据时应删除对应密文及专用 Keystore 密钥，并使此前排队的写入失效，避免数据被异步任务“复活”。
- 安全剪贴板默认关闭。每次正文读取都需要 Android 生物识别或设备凭据认证产生的一次性授权；授权不得复用、序列化或跨会话保存。
- 认证请求必须绑定发起时的会话令牌、原始 `InputConnection`、交互序号和可取消句柄。发起认证后如切换应用/输入框、结束会话、继续输入、切换面板、修改设置/语言包/子类型，结果必须失效。
- 至少在认证回调、后台读取开始和解密完成准备提交三个边界重新校验请求身份；失效结果静默丢弃，绝不能提交到新的编辑器。

涉及密码学、认证、权限、导入或数据删除的变更必须更新威胁分析和负向测试；仅验证成功路径不算完成。

## 5. 架构与依赖方向

依赖方向以 `docs/architecture.md` 为准：

```text
app -> ime-core --------> engine-api
 |                         ^
 +-> ime-ui                |
 +-> engine-english -------+
 +-> engine-rime ----------+
 +-> user-data ------------+
 +-> language-pack --------+

user-data -----> security
language-pack -> security
```

### 5.1 模块职责

| 模块 | 单一职责 | 不应承担 |
| --- | --- | --- |
| `app` | Android 组件、组合根、生命周期编排、平台适配、设置入口 | 引擎算法、通用加密实现、可复用输入规则 |
| `engine-api` | 稳定的引擎与个性化端口、不可变模型 | Android 类型、Rime 类型、持久化实现、UI |
| `engine-english` | 离线英语候选与英语引擎实现 | Android 生命周期、用户数据落盘 |
| `engine-rime` | librime/JNI 适配、Rime 运行时、中文全拼资源与降级实现 | 向业务层泄漏 Rime API、存储个人词典 |
| `ime-core` | 与 UI/引擎实现无关的会话状态机、命令处理、隐私策略 | Android View、具体仓库、librime 类型 |
| `ime-ui` | 键盘、候选、emoji 与安全剪贴板面板的展示和用户意图 | 磁盘 I/O、解密、学习策略、引擎选择策略 |
| `language-pack` | 纯数据语言包的解析、校验、安装、注册和数据型引擎 | 动态代码、DEX/JAR、脚本或 native 库加载 |
| `security` | Keystore、认证授权模型、加密文件原语 | 业务数据结构、界面和输入会话策略 |
| `user-data` | 加密用户词库、emoji 历史、安全剪贴板仓库 | UI、系统剪贴板、网络、平台输入会话编排 |

### 5.2 依赖规则

- 依赖必须指向稳定抽象。跨模块契约放在拥有该抽象的模块，不能为方便把所有接口堆入 `app`。
- `ime-core` 和业务层只依赖 `engine-api/InputEngine`、`InputEngineFactory`、`PersonalizationStore` 等自有端口，不得引用 librime、JNI 或其数据结构。
- librime C API 变化只能传播到 `engine-rime` 的 `NativeRimeBridge`、JNI 和资产部署边界。
- `user-data` 依赖 `security` 提供的加密原语；`security` 不反向理解词组、emoji 或剪贴板业务。
- 不允许循环依赖。新增模块前先证明它具有独立职责、稳定边界和真实复用价值，不能只为缩短文件而创建模块。
- 新语言优先通过语言包或新的 `InputEngineFactory` 注册扩展，不在会话控制器中加入语言特例分支。

### 5.3 可替换引擎契约

- `InputEngine` 是长期稳定的 Kotlin 端口。引擎快照和更新保持不可变，提交文本与组合状态由 `ime-core` 统一协调。
- 每个引擎必须正确实现 `start`、`handle`、候选选择、翻页、`reset` 和 `close`，并通过一致的契约测试。
- 引擎不可用、native 初始化失败或无法创建会话时必须显式进入失败状态并使用受控降级路径，不得崩溃或悄悄丢键。
- Release 构建必须使用 `-PrequireRime=true`，防止把仅含降级拼音引擎的包误当正式产物。
- librime 自带用户词典保持关闭。中文拼音输入码和候选频率只能通过 ZeroInput 的加密 `PersonalizationStore` 学习。

## 6. 输入线程、并发与生命周期

输入法是延迟敏感组件。以下工作严禁在 IME 主线程或按键热路径执行：

- 磁盘读写、完整词典扫描、JSON/ZIP 大文件解析。
- Keystore 操作、加密或解密、安全剪贴板正文读取。
- librime/native 首次初始化、语言包完整性扫描。
- 无界集合遍历、阻塞等待、线程休眠或同步等待后台结果。

并发实现必须满足：

- 输入主线程只读取已准备好的内存快照，并尽快返回；冷启动工作放入有界、可取消的后台执行器。
- 同一持久化资源的写入串行化，避免乱序覆盖。后台任务必须捕获会话令牌或数据代次，并在执行前与提交前校验。
- 会话结束、隐私收紧、清除数据和组件销毁时取消相关任务、提升代次并释放引擎、线程、认证句柄等资源。
- 不持有已销毁的 `Activity`、View、`InputConnection` 或输入会话引用。跨线程暴露不可变值或明确同步的状态。
- 异步结果必须针对过期、重复回调、乱序完成、执行器拒绝和异常路径具有确定行为。
- 性能修复应先测量热路径；不得用牺牲隐私、正确性或无界缓存换取表面速度。

## 7. Android 组件与界面规范

- 所有新增 Activity、Service、Receiver、Provider 默认 `android:exported="false"`。
- 仅启动 Activity 和受 `android.permission.BIND_INPUT_METHOD` 保护的 IME Service 可作为当前系统入口。新增导出组件必须先更新威胁模型并获得明确确认。
- 不申请存储、通知、位置、联系人等与离线输入无关的权限。新增任何权限都必须说明必要性并同步更新 `privacyCheck` 白名单；白名单不得为了让构建通过而放宽。
- 使用现有 Android Views、ViewBinding、Material 与资源体系。可见字符串放入资源，不硬编码；触控目标、无障碍描述、横竖屏、深浅主题和小屏布局均需可用。
- 输入面板优先保证稳定尺寸与快速操作。动态内容不得导致键盘、候选条或工具按钮无意义跳动、重叠或溢出。
- 二元设置使用开关/复选框，模式使用分段或单选控件，熟悉操作优先使用已有图标并提供内容描述。避免装饰性卡片、嵌套卡片和与输入效率无关的动画。
- 不在界面中暴露内部实现说明、调试信息或安全机理教程；用户需要看到的是清晰状态、风险提示和可执行操作。

## 8. 语言包与导入边界

- 语言包只能包含声明清单允许的数据文件。禁止动态代码、DEX/JAR、可执行脚本、共享库、native 库及可绕过解析器的未知文件类型。
- 所有导入入口必须限制压缩包/文件大小、解压后总大小、条目数、单条长度和嵌套深度，并防御 zip slip、zip bomb、符号链接、绝对路径、路径穿越和重复名称。
- 清单及每个文件需验证格式、版本、BCP-47 标签、相对路径、扩展名、长度和校验和；先完整验证到临时位置，再原子激活。
- 当前仅接受明确映射为 `zh` 或 `en` 的标签，且启用前必须确认至少存在一条可用词典数据。扩展语种时应同时扩展模型、解析校验、引擎注册和测试，不能只放宽字符串判断。
- 无效或不兼容包必须失败关闭，并保留原有可用状态；错误消息不得包含敏感文件内容。

## 9. librime 与第三方依赖

- librime 及其递归依赖必须固定到明确 tag/commit；下载归档和语言数据必须固定 SHA-256。禁止使用浮动分支、`latest` 或未校验下载。
- 通过 `tools/bootstrap-rime.ps1` 获取受控上游源码。不要直接修改 `third_party/` 来掩盖适配问题；上游差异应尽量由 `engine-rime` 的 CMake/JNI/适配器吸收。
- 更新 librime、Boost、OpenCC、LevelDB、yaml-cpp、marisa-trie 或其他第三方内容时，必须同步检查 `THIRD_PARTY.md`、`NOTICE`、`LICENSES/`、固定版本、哈希和构建脚本。
- 新依赖必须有清晰必要性、离线行为、许可证兼容性、维护状态、体积和供应链风险评估。能用标准库或已有依赖清楚解决时不新增依赖。
- 不引入会隐式初始化联网、采集、备份或跨进程组件的 SDK。

## 10. 代码质量标准

### 10.1 设计原则

- 以单一职责、开闭原则、里氏替换、接口隔离、依赖倒置为基准，保持 DRY、高内聚、低耦合。
- 优先采用仓库现有命名、状态模型、构造注入和端口适配模式。业务规则与 Android 生命周期、存储和 UI 分离。
- 抽象必须消除真实重复、隔离不稳定依赖或形成明确测试边界。不要为假想需求建立通用框架。
- 使用结构化解析器处理 JSON、清单、ZIP 和路径；不要依赖脆弱的字符串拼接与正则替代格式解析。
- 公共 API 保持最小且明确；默认限制可见性。错误和空状态使用可判定模型，不以日志或吞异常代替处理。

### 10.2 文件与函数规模

- 单个源码文件一般不得超过 1500 行；复杂且业务上无法合理拆分时上限为 2500 行。
- 单个函数或方法不得超过 100 行。
- 接近限制时按职责、状态所有权和依赖边界拆分，不按行数机械切文件。确因业务约束必须超出时，在交付说明中指出原因、风险和后续治理方式。
- 注释解释约束、竞态或不直观的设计原因，不逐行复述代码。公共契约在名称不足以表达前置条件时补充简洁 KDoc。

### 10.3 Kotlin、C++ 与资源

- Kotlin 优先使用不可变数据、穷尽 `when`、显式空值处理和小型接口。避免 `!!`、全局可变状态、无界协程/线程和隐藏副作用。
- 捕获异常应缩小范围并保持取消语义；安全失败采用 fail-closed，输入引擎失败采用已定义的可用降级路径。
- JNI 层保持薄、可审计，验证所有边界参数和生命周期；native 资源遵循 RAII，不把裸指针或 Rime 类型泄漏到 Kotlin 业务层。
- 新增面向用户文本时维护中文与英文资源的一致性；代码标识符和新增技术文档默认使用 ASCII，已有中文文档与字符串可继续使用中文。
- 不提交生成目录、构建缓存、本机 SDK 路径、密钥、签名文件或包含用户数据的样本。

## 11. 测试策略

测试随风险和影响范围扩展，不以“能编译”替代行为验证。

### 11.1 按改动范围选择测试

| 改动 | 最低验证要求 |
| --- | --- |
| 纯文档 | 校验链接、路径、命令和文档间一致性 |
| 单一纯 Kotlin 模块 | 对应模块单元测试，必要时加根级 `testDebugUnitTest` |
| `ime-core` 或引擎契约 | 相关模块测试 + `:ime-core:testDebugUnitTest` + 所有引擎契约/回归测试 |
| Manifest、权限、依赖、备份或剪贴板相关 | `privacyCheck` + Debug/Release 合并清单检查 + `:app:lintDebug` |
| 加密、用户数据、认证或异步队列 | 单元/集成负向测试 + 竞态与代次测试 + 清除/损坏/密钥失效路径 |
| JNI、librime、CMake 或 ABI | 三个 ABI 构建/打包检查 + native 失败降级 + `-PrequireRime=true` |
| 输入面板、设置页或资源 | 单元测试 + Lint + Debug APK + 目标尺寸/主题/旋转的真机或模拟器检查 |
| 发布相关 | 全量测试、隐私检查、Release 构建、许可证与产物检查 |

### 11.2 必须覆盖的高风险场景

- 密码、PIN、可见密码、邮箱、URI、无个性化标志和运行中收紧隐私设置。
- 会话切换、旧 `InputConnection`、认证重复/延迟回调、认证后继续输入、面板切换和服务销毁。
- 清除数据与排队学习/emoji 写入并发，旧代次不能重新生成已删除数据。
- 加密文件截断、篡改、版本不匹配、Keystore 密钥丢失或失效，以及明文缓冲区清理。
- 语言包路径穿越、绝对路径、重复项、超限、校验和错误、未知类型、空词典和不支持语言。
- 引擎启动、组合、提交、退格、空格、回车、候选索引、翻页、重置、关闭和 native 不可用。
- emoji 搜索、最近使用排序、隐私隐藏与本地清除。

测试名称描述可观察行为与条件。修复缺陷时先添加能稳定复现问题的回归测试，除非平台限制使其无法自动化；无法自动化的部分需记录手工验收步骤。

## 12. 构建与验证命令

Windows/PowerShell 环境使用仓库内 Gradle Wrapper。

首次准备固定版本的 librime 与依赖：

```powershell
./tools/bootstrap-rime.ps1
```

日常完整验证：

```powershell
./gradlew.bat :app:assembleDebug testDebugUnitTest privacyCheck :app:lintDebug `
  "-Pandroid.injected.build.abi=x86_64"
```

需要验证真实 Rime 源码时添加 `-PrequireRime=true`。不要用缺失 native 源码后的降级构建替代 Rime 相关验证。

生成并校验真机测试包：

```powershell
./tools/package-test-apk.ps1
```

该脚本运行测试、隐私检查和 Lint，并检查 APK 包名、ABI、权限与签名，最后生成 SHA-256 清单。可用 `-Abi arm64-v8a` 等参数生成单 ABI 包；只有已单独完成等价检查时才可使用 `-SkipChecks`。

Release 验证：

```powershell
./gradlew.bat :app:assembleRelease -PrequireRime=true --no-parallel
```

Release APK 当前未配置正式签名。代理不得生成、提交或假定持有发布签名密钥，也不得把 Debug 签名产物描述为可发布版本。

若因环境、SDK、NDK、设备或耗时限制未运行某项要求的验证，交付时必须明确列出未运行项、原因和剩余风险；不得声称通过。

## 13. 文档与决策记录

- 架构、依赖方向、运行时边界变化时同步更新 `docs/architecture.md`。
- 受保护资产、攻击面、安全控制或已知边界变化时同步更新 `docs/threat-model.md` 和 `SECURITY.md`。
- 难以逆转、影响多个模块或改变安全模型的决策新增 ADR；ADR 记录背景、决定、后果和替代方案，不只记录最终代码。
- 用户可见功能、构建方式、安装与验收步骤变化时同步更新 `README.md`。
- 第三方版本或许可证变化时同步更新 `THIRD_PARTY.md`、`NOTICE` 和 `LICENSES/`。
- 文档中的命令必须能从仓库根目录执行；路径、模块名、属性名和当前代码保持一致。

## 14. 禁止以通过检查为目的削弱规则

不得进行以下操作：

- 删除、跳过或放宽 `privacyCheck`、Lint、测试、`requireRime` 或导入边界，只为让变更通过。
- 使用反射、字符串拼接、native 绕行或重命名来规避系统剪贴板与权限扫描。
- 在敏感输入中保留旧候选、延迟学习或认证结果，声称稍后会清理。
- 把秘密正文放入列表摘要、通知、最近任务、日志、异常、数据库索引或长期内存缓存。
- 在主线程等待后台初始化，或在生命周期结束后仍向编辑器提交结果。
- 让 `app`、`ime-core` 或 UI 直接调用 librime/JNI，或让语言包执行任意代码。
- 为未知未来兼容性保留重复实现、双写数据或无期限的旧接口。
- 修改无关文件、覆盖用户已有改动，或在未经明确请求时执行破坏性版本控制操作。

## 15. 完成定义

提交或交付前逐项确认：

- 行为满足已确认需求，且明确的非目标没有被顺带加入。
- 隐私、剪贴板、备份、认证、敏感输入和离线边界未被削弱。
- 依赖方向正确，职责位于所属模块，未向业务层泄漏平台或 Rime 细节。
- 主线程无新增 I/O、Keystore、解密、完整扫描或 native 初始化；异步结果具备会话/代次失效保护。
- 新增输入、文件和跨组件边界均有限制、校验和失败关闭行为。
- 代码规模、命名、资源、可访问性和生命周期符合本文件要求。
- 按改动风险补齐并运行测试、`privacyCheck`、Lint 和构建；失败与未运行项如实说明。
- 相关架构、安全、用户文档和第三方声明已同步。
- 交付说明简洁列出变更、验证结果、未验证项和剩余风险，不夸大完成度。
